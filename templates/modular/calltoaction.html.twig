<!-- Call to action-->
<section class="page-section bg-dark text-white">
	<div class="container text-center">
		<h2 class="mb-4">{{ page.header.title }}</h2>
		{% for button in page.header.buttons %}
			<a class="btn btn-light btn-xl" target="_blank" rel="noopener noreferrer" href="{{ button.url }}">{{ button.text }}</a>
		{% endfor %}
	</div>
</section>
<!-- Contact-->